Scaffold
--------

A Python scaffold for running experiments on iterative stochastic algorithms.

Features:
* Run experiments remotely, via picloud
* ...others...

To see documentation, cd to the doc directory and execute 'make html' on the command line. Then open doc/_build/html/index.html in a web browser.